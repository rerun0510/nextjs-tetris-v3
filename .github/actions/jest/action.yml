name: jest

runs:
  using: 'composite'
  steps:
    - name: Install dependencies 👨🏻‍💻
      run: npm install
      shell: bash

    - name: Run tests 🧪
      run: npm run test:ci
      shell: bash
